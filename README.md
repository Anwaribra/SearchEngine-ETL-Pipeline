# SearchEngine-ETL-Pipeline

## Overview
SearchEngine-ETL-Pipeline is an end-to-end data engineering project that simulates a **search engine backend**.  
It processes synthetic search query logs, transforms raw events into analytics-ready datasets using **Databricks (Apache Spark)**, and delivers insights through BI dashboards.